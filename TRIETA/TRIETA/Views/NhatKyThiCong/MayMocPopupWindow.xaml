<Window x:Class="Trita.Views.NhatKyThiCong.MayMocPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Khai báo máy móc/thiết bị sử dụng"
        Width="560" Height="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="HeaderCell" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="RoundBtn" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="KHAI BÁO MÁY MÓC / THIẾT BỊ SỬ DỤNG"
                   FontSize="15" FontWeight="Bold"
                   Foreground="#2E4057"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16"/>

        <!-- Header -->
        <Border Grid.Row="1" Background="#C0692A" CornerRadius="8,8,0,0" Padding="0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="45"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="STT" Style="{StaticResource HeaderCell}"/>
                <TextBlock Grid.Column="1" Text="Tên máy / Thiết bị" Style="{StaticResource HeaderCell}"/>
                <TextBlock Grid.Column="2" Text="Số lượng" Style="{StaticResource HeaderCell}"/>
                <TextBlock Grid.Column="3" Text="Ca máy" Style="{StaticResource HeaderCell}"/>
                <TextBlock Grid.Column="4" Text="" Style="{StaticResource HeaderCell}"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" BorderBrush="#CCCCCC" BorderThickness="1,0,1,1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="DanhSachMayMocPanel"/>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="3" Background="#FDF0E8" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="0,0,8,8" Padding="0,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="45"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Grid.ColumnSpan="2"
                           Text="Tổng:"
                           FontWeight="Bold" FontSize="11"
                           HorizontalAlignment="Right" Margin="0,0,8,0"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="TongSoLuongTextBlock" Grid.Column="2"
                           Text="0" FontWeight="Bold" FontSize="11"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button Grid.Column="4" Content="+"
                        Width="28" Height="28"
                        Background="#C0692A" Foreground="White"
                        FontWeight="Bold" FontSize="16"
                        Style="{StaticResource RoundBtn}"
                        Margin="8,0"
                        Click="ThemDong_Click"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Hủy" Width="90" Height="34" Background="#CCCCCC" Foreground="#333"
                    Style="{StaticResource RoundBtn}" Padding="0" Margin="0,0,10,0" Click="Huy_Click"/>
            <Button Content="Xác nhận" Width="100" Height="34" Background="#C0692A" Foreground="White"
                    Style="{StaticResource RoundBtn}" Padding="0" Click="XacNhan_Click"/>
        </StackPanel>
    </Grid>
</Window>
